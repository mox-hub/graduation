export interface DescriptionsVO {
    batch_size: number,
    steps: number,
    sampler_index: string,
    cfgScale: number,
    size: string,
    width: number,
    height: number,
    rate: number,
    isShared: number,
}

